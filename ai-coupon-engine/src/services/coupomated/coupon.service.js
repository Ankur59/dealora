import { getAllCoupons } from "../../providers/coupomated"

const syncAllCoupons = async () => {
    const allCoupons = await getAllCoupons()
    const parsedCoupons=allCoupons.map((item)=>{
        normaliz
    })
}